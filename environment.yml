name: dem-stitcher
channels:
 - conda-forge
 - anaconda
 - defaults
dependencies:
 - python>=3.9
 - pip
 - affine
 - boto3
 - build
 - dateparser
 - fiona
 - flake8
 - flake8-blind-except
 - flake8-builtins
 - flake8-import-order
 - gdal
 - geopandas
 - jinja2
 - joblib
 - jupyter
 - matplotlib
 - numpy
 - pytest
 - pytest-mock
 - rasterio
 - setuptools>=61.0
 - setuptools_scm>=6.2
 - shapely
 - tqdm
